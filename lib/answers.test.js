// Test for validating user answers to questions
const index = require('../index.js');

describe("Answers", () => {
  describe("text length", () => {
    it("raises error if text is longer than 3 characters", () => {
      const textLength = answers.text.length;
      // expect abcd to return false
      // expect("abcd").toEqual(false);
    });
  });
});